@model IEnumerable<Agenda.Models.Book>

@{
    ViewData["Title"] = "Index";
}

<h1>Agendas</h1>

<p>
    <a asp-action="Create">Inserir Agenda</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a asp-controller="Contato" asp-action="Index" asp-route-id="@item.Id">Contatos</a> |
                <a asp-action="Update" asp-route-id="@item.Id">Editar</a> |
                <a asp-action="Remove" asp-route-id="@item.Id">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
